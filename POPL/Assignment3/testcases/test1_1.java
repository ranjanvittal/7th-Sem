A <= B ? 
B <= C ?
B <= D ?
D <= A ?
D <= C ?

interface A {
	int ask(boolean m);
	boolean mnt(int a);
	int mint(B a);
}

interface B{
	boolean mnt(int x);
	int ask(boolean n);
	boolean prop(D x);
}

interface C{
	void gist(int x);
	void gets(boolean r);
}

interface D{
	int set(boolean v);
	void gets(boolean r);
	void gist(int m);
}
